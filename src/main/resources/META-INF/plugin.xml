<!-- Plugin Configuration File: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN, cannot be changed between the plugin versions. -->
    <id>de.espend.ml.llm</id>
    <version>0.6.0</version>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-name -->
    <name>AI Assistant Providers / Sessions</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://github.com/espend">espend</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-description -->
    <description><![CDATA[
        <p>Configure and use multiple AI providers inside IntelliJ IDEA's AI Assistant chat Agent and commit message generation. Includes a Session Browser with MCP for viewing / search chat sessions from external AI tools, and a Usage Toolbar to monitor AI provider quotas.</p>

        <h2>AI Providers</h2>
        <p>Configure multiple AI providers through **Settings → AI Assistant → Agent Providers**.</p>

        <h2>Session Browser</h2>
        <p>Browse and view chat sessions from external AI tools directly in the IDE.</p>

        <h2>Provider Usage Toolbar</h3>
        <p>Monitor your AI provider quota usage across multiple accounts directly from the toolbar</p>

        <h2>Supported Session Sources</h3>
        <p>The Session Browser is available in the AI Assistant chat toolbar and in the right sidebar.</p>

        <table>
            <tr><th>Source</th><th>Description</th></tr>
            <tr><td><b>Claude Code</b></td><td>Chat sessions from Claude Code CLI</td></tr>
            <tr><td><b>Codex CLI</b></td><td>Chat sessions from JetBrains AI Assistant CLI</td></tr>
            <tr><td><b>OpenCode</b></td><td>Chat sessions from OpenCode CLI</td></tr>
            <tr><td><b>Amp</b></td><td>Chat sessions from ampcode.com</td></tr>
            <tr><td><b>Junie</b></td><td>Chat sessions from Junie CLI (JetBrains)</td></tr>
            <tr><td><b>Gemini</b></td><td>Chat sessions from Gemini CLI (Google)</td></tr>
            <tr><td><b>Droid</b></td><td>Chat sessions from Factory.ai CLI</td></tr>
            <tr><td><b>Kilo Code</b></td><td>Chat sessions from Kilo Code CLI</td></tr>
        </table>

        <h2>MCP Integration (IntelliJ Built-in MCP Server)</h3>
        <p>The plugin exposes AI session data via IntelliJ's built-in MCP server, allowing any MCP client to search and retrieve past coding sessions.</p>
        <p><b>Requirement:</b> The <code>com.intellij.mcpServer</code> plugin must be enabled in your IDE.</p>
        <table>
            <tr><th>Tool</th><th>Description</th></tr>
            <tr><td><b>search_ai_sessions</b></td><td>Search across all AI sessions for a query string. Returns matching sessions with text snippets.</td></tr>
            <tr><td><b>get_ai_session</b></td><td>Fetch the full conversation of a session by its ID.</td></tr>
        </table>

        <h2>Using AI Providers</h3>
        <ol>
            <li>Go to <b>File → Settings → AI Assistant → Agent Providers</b></li>
            <li>Configure API keys and base URLs as needed</li>
            <li>Click <b>Apply</b> to register the agents</li>
            <li>For CLI-based providers: install the corresponding npm package</li>
            <li><b>Important</b>: To use providers for core AI features (commit generation, AI actions):
                <ul>
                    <li>Go to <b>Settings → Tools → AI Assistant → "Models & API keys"</b></li>
                    <li>Select <b>"Agent Providers"</b> from the provider dropdown</li>
                    <li>Select a model from the model dropdown</li>
                </ul>
            </li>
        </ol>

        <h2>Provider Usage Toolbar</h3>
        <p>Monitor your AI provider quota usage across multiple accounts directly from the toolbar. Click the usage icon in the main toolbar to see current usage percentages and reset times.</p>
        <p><b>Location</b> (when configured): Main toolbar (right side) → Usage icon</p>
        <p>Configure accounts in <b>Settings → Tools → Account Usage Toolbar</b>.</p>

        <table>
            <tr><th>Provider</th><th>Quota Type</th><th>Credential Modes</th></tr>
            <tr><td><b>Claude</b></td><td>5-hour window + 7-day window</td><td>Auto (<code>~/.claude/.credentials.json</code>), Manual (access token), Web API (sessionKey)</td></tr>
            <tr><td><b>Codex</b></td><td>Rate limit quota</td><td>Auto (<code>~/.codex/auth.json</code>), Manual (refresh token)</td></tr>
            <tr><td><b>Z.AI</b></td><td>Token quota</td><td>API key</td></tr>
            <tr><td><b>Ampcode</b></td><td>Free credits balance</td><td>Auto (<code>~/.local/share/amp/secrets.json</code>), Manual (API key)</td></tr>
        </table>
    ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.ml.llm</depends>
    <depends config-file="mcp.xml" optional="true">com.intellij.mcpServer</depends>

    <!-- Extensions defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <!-- AI Provider Agent Registry - dynamic agent registration -->
        <applicationService serviceImplementation="de.espend.ml.llm.AgentRegistry"/>

        <!-- Settings for AI provider agents - placed under AI Assistant -->
        <applicationConfigurable parentId="ml.llm.LLMConfigurable"
                                 id="de.espend.ml.llm.AgentSettings"
                                 instance="de.espend.ml.llm.AgentSettingsConfigurable"
                                 displayName="Agent Providers"
                                 groupWeight="10"/>

        <applicationConfigurable parentId="de.espend.ml.llm.AgentSettings"
                                 id="de.espend.ml.llm.usage.UsageSettings"
                                 instance="de.espend.ml.llm.usage.ui.UsageSettingsConfigurable"
                                 displayName="Account Usage Toolbar"
                                 groupWeight="20"/>

        <postStartupActivity implementation="de.espend.ml.llm.AgentStartupActivity"/>

        <!-- Agent Popup Enhancement - adds Agent Providers to agent dropdown -->
        <ml.llm.core.chat.ui.agentPopupEnhancementProvider implementation="de.espend.ml.llm.ProviderAgentPopupEnhancementProvider"/>

        <!-- Session Browser Tool Window -->
        <toolWindow id="Session Browser"
                    factoryClass="de.espend.ml.llm.session.SessionBrowserToolWindowFactory"
                    anchor="right"
                    icon="/icons/ai-provider.svg"/>
    </extensions>

    <!-- Actions for AI Provider plugin -->
    <actions>
        <!-- Action to open Agent Providers settings from chat toolbar -->
        <action id="de.espend.ml.llm.OpenAgentProvidersSettings"
                class="de.espend.ml.llm.OpenAgentProvidersSettingsAction"
                text="Agent Providers"
                description="Open Agent Providers settings">
            <add-to-group group-id="AIAssistant.ToolWindow.Chat.SecondaryGearActions" anchor="after" relative-to-action="Acp.OpenConfiguration"/>
        </action>

        <!-- Action to open Session Browser from Tools menu -->
        <action id="de.espend.ml.llm.OpenSessionBrowser"
                class="de.espend.ml.llm.session.OpenSessionBrowserAction"
                text="AI Session Browser"
                description="Browse AI chat sessions">
            <add-to-group group-id="AIAssistant.ToolWindow.Chat.SecondaryGearActions" anchor="after" relative-to-action="Acp.OpenConfiguration"/>
        </action>

        <!-- Provider Usage Action - shows usage in main toolbar -->
        <action id="de.espend.ml.llm.ProviderUsage"
                class="de.espend.ml.llm.usage.action.ProviderUsageAction"
                text="Usage"
                description="View provider usage and quota">
            <add-to-group group-id="MainToolbarRight" anchor="last"/>
        </action>

        <!-- Commit Message Generator Action -->
        <action id="de.espend.ml.llm.ProviderCommitMessageAction"
                class="de.espend.ml.llm.vcs.ProviderCommitMessageAction"
                text="Generate with Agent Provider"
                description="Generate commit message using configured AI providers">
            <add-to-group group-id="Vcs.MessageActionGroup" anchor="after" relative-to-action="Vcs.LLMCommitMessageAction"/>
        </action>
    </actions>
</idea-plugin>
